# git 계정 두개 사용 시 ssh 설정
```
git 계정은 https 연결과 ssh 연결 방식 2개가 있다.

https 연결 시
(윈도우)
제어판 -> 자격 증명 관리자 -> Windows 자격 증명
git:https://github.com
여기에 계정이 자동으로 등록되어
이 계정으로 https 연결을 수행한다.

따라서 매번 계정을 바꾸는데 번거로운 작업이 필요하므로

한번에 2개를 관리할 수 있는 방법(하지만 기존의 https 연결 방식과 차이가 생김)
```
[GitHub(SSH연결)](./GitHub(SSH%20연결).md)